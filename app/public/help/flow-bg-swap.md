---
tags: [flow-control-export]
---
# Composite onto Background

Aggregation. Image 1 = foreground subject (place `ai-remove-bg` upstream to cut it out). Images 2+ = background scenes (used original, unprocessed). Composites the cutout subject onto each background, producing one output per background. Use with ordered selection.

